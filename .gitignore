# Nix
# Risultato di `nix build` e `nix develop`
result
result-*
.direnv/
# Nix store locale (non dovrebbe essere in repo, ma per sicurezza)
nix/store

# Secrets
*.pem
*.key
.env
certs/
# Age private key (mai committare)
age-key.txt
keys.txt
# SOPS placeholder (verrà sostituito con la vera chiave al primo encrypt)
AGE_PUBLIC_KEY

# Kubernetes
kubeconfig
*.kubeconfig

# Editor / OS
*.swp
*.swo
.DS_Store
